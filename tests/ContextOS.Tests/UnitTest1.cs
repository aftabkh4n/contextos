namespace ContextOS.Tests;

public class ScaffoldTests
{
    [Fact]
    public void Placeholder_passes()
    {
        // This test exists only to confirm the project compiles and runs.
        // Replace with real tests as features are added.
        Assert.True(true);
    }
}
